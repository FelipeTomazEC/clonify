import styled from "styled-components";

export const Nav = styled.nav`
  margin-top: 12px;

  ul {
    list-style: none;
    display: flex;
    justify-content: flex-start;
    height: 2rem;

    li {
      margin-right: 28px;
    }
  }
`;
